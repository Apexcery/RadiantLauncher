<UserControl
    x:Class="Radiant.Views.ContentViews.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:viewModels="clr-namespace:Radiant.ViewModels"
    mc:Ignorable="d" 
    d:DesignWidth="1280" d:DesignHeight="720"
    Background="{DynamicResource BackgroundLight}"
    d:DataContext="{d:DesignInstance Type=viewModels:HomeViewModel}">
    <UserControl.Resources>
        <materialDesign:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" TrueValue="Visible" FalseValue="Hidden"/>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="250" />
        </Grid.ColumnDefinitions>
        
        <!-- Left Panel -->
        <Grid Grid.Column="0">
            <Grid ClipToBounds="True">
                <Image
                    Stretch="UniformToFill"
                    HorizontalAlignment="Center"
                    Source="{StaticResource HomeBackgroundImage}"
                    Margin="0, -15">
                    <Image.Effect>
                        <BlurEffect Radius="20" RenderingBias="Performance" />
                    </Image.Effect>
                </Image>
            </Grid>
        </Grid>

        <!-- Right Panel -->
        <Grid
            Grid.Column="1"
            Background="{DynamicResource ValorantWhite}">

            <!-- Login Form -->
            <Grid
                x:Name="LoginForm"
                d:Visibility="Visible"
                Visibility="{Binding LogInFormVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                HorizontalAlignment="Center"
                Width="250px">

                <StackPanel
                    Orientation="Vertical"
                    Margin="5"
                    Width="240px"
                    HorizontalAlignment="Center">
                    
                    <TextBlock
                        Text="Sign In"
                        HorizontalAlignment="Center"
                        FontFamily="{StaticResource RobotoSlabBold}"
                        FontSize="{StaticResource LoginFormTitleTextSize}"
                        FontWeight="Bold"
                        Margin="0, 50"/>

                    <TextBox
                        x:Name="TxtUsername"
                        Tag="Username"
                        Text="{Binding UsernameText}"
                        Style="{StaticResource LoginFormTextBox}"
                        materialDesign:HintAssist.Hint="Username"
                        materialDesign:HintAssist.FontFamily="{StaticResource RobotoSlabBold}" />
                    <PasswordBox
                        x:Name="TxtPassword"
                        Tag="Password"
                        PasswordChar="*"
                        Style="{StaticResource LoginFormPasswordBox}"
                        materialDesign:HintAssist.Hint="Password"
                        materialDesign:HintAssist.FontFamily="{StaticResource RobotoSlabBold}" />

                    <CheckBox
                        Style="{StaticResource MaterialDesignUserForegroundCheckBox}"
                        FontFamily="{StaticResource RobotoSlabBold}"
                        Foreground="{DynamicResource BackgroundDark}"
                        IsChecked="{Binding Path=LoginAutomatically, Mode=TwoWay, FallbackValue=False}"
                        Command="{Binding LoginAutomaticallyCommand}"
                        FontWeight="Bold"
                        Content="Login Automatically"
                        HorizontalAlignment="Center"
                        Margin="0, 10, 0, 0" />
                    <!-- Checked="LoginAutomatically_OnChecked" -->

                    <Button
                        Margin="0, 15, 0, 0"
                        Content="Sign In"
                        Width="150"
                        Height="50"
                        Command="{Binding LoginCommand}"
                        CommandParameter="{Binding ElementName=TxtPassword}"
                        IsDefault="True"
                        Background="{DynamicResource ValorantRed}"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        materialDesign:ButtonAssist.CornerRadius="5" />
                </StackPanel>
            </Grid>

            <!-- Loading Spinner -->
            <Grid
                d:Visibility="Hidden"
                Visibility="{Binding LoadingVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                HorizontalAlignment="Center"
                Width="250px">
                
                <ProgressBar
                    Style="{DynamicResource MaterialDesignCircularProgressBar}"
                    Width="50"
                    Height="50"
                    IsIndeterminate="True"
                    Foreground="{DynamicResource ValorantRed}" />
            </Grid>

            <!-- Play Button Form -->
            <Grid
                d:Visibility="Hidden"
                Visibility="{Binding PlayFormVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                HorizontalAlignment="Center"
                Width="250px">

                <Button
                    Margin="0, 0, 0, 150"
                    Content="Play"
                    Width="150"
                    Height="50"
                    Command="{Binding PlayCommand}"
                    Background="{DynamicResource ValorantRed}"
                    BorderBrush="Transparent"
                    BorderThickness="0"
                    materialDesign:ButtonAssist.CornerRadius="5" />

                <TextBlock
                    Text="Signed in as: "
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    FontFamily="{StaticResource RobotoSlabBold}"
                    FontSize="{StaticResource PlayFormTitleTextSize}"
                    FontWeight="Medium"
                    Margin="0, 55"
                    MaxWidth="240">
                    <Run
                        Text="{Binding GameNameText}" />
                </TextBlock>

                <Button
                    Margin="0, 0, 0, 15"
                    Content="Sign Out"
                    Width="150"
                    Height="35"
                    Command="{Binding LogoutCommand}"
                    CommandParameter="{Binding ElementName=TxtPassword}"
                    Background="{DynamicResource ValorantRed}"
                    BorderBrush="Transparent"
                    BorderThickness="0"
                    VerticalAlignment="Bottom"
                    materialDesign:ButtonAssist.CornerRadius="5" />

            </Grid>
        </Grid>
    </Grid>
</UserControl>
